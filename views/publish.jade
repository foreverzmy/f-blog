extends side

block article
  form(method="POST")
    label(for="title") title：
      input(type="text" name="title")
    br
    label(for="main") main：
      textarea(name="main", cols="100", rows="20")
    br
    input(type="submit" value="submit")